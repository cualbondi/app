const initial = {
  initialRegion: {
    latitude: -34.9205,
    longitude: -57.9536,
    latitudeDelta: 0.0922,
    longitudeDelta: 0.0421
  }
}

export default (state = initial, action) => {
  return state
}